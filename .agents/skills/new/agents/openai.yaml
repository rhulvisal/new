interface:
  display_name: "New"
  short_description: "Repo-specific patterns and workflows for new"
  default_prompt: "Use the new repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true